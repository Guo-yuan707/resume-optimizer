# 虚拟环境 — 每个机器自己装，不用上传
.venv/

# 密钥文件 — 包含 API Key，绝不能泄露
.env

# Python 缓存 — 自动生成，没用
__pycache__/
*.pyc

# pytest 缓存 — 跑测试自动生成，没用
.pytest_cache/
**/.pytest_cache/

# 导出文件 — 程序生成的结果，没必要上传
output/

# 真实个人资料 — 含手机号/邮箱，绝不进公开仓库
examples/my-resume.txt
examples/my-jd.txt
