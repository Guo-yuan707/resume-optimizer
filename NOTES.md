# 学习笔记

> 跟着项目进度更新。每一课 = 一个功能模块 + 若干 Python 知识点。
> 学完每一课,合上笔记,自己能把知识点讲出来,才算真的会了。

## 第 1 课:项目骨架与第一个程序

### 1. Python 项目的结构
- `.py` 文件 = Python 源代码文件
- 带 `__init__.py` 的文件夹叫「包(package)」,包里的 `.py` 叫「模块(module)」
- 模块之间用 `import` 互相引用,点号表示一层层往里走:
  `from resume_optimizer.parser import read_text`
  = 从 `resume_optimizer` 包 → `parser` 模块 → 拿 `read_text` 这个函数

### 2. 虚拟环境 venv(面试常问)
- 问题:不同项目的第三方库版本可能冲突
- 方案:venv 给每个项目一个独立环境,库互不干扰
- 命令(只建一次):`python -m venv .venv`
- 进入环境:`.venv/Scripts/activate`(命令行前缀出现 `(.venv)` 说明生效)
- 离开环境:`deactivate`
- 也可以不激活,直接每次用 `.venv/Scripts/python` 来运行

### 3. 函数 def
- `def 函数名(参数):` 定义一段可复用的代码
- `path: str -> str` 是类型注解,只是给 IDE 和读者看的提示,不影响运行
- 好处:把「读文件」这件事封装成一次,主程序一行调用,结构清晰

### 4. with open() 读文件
- `open(path, "r", encoding="utf-8")`:打开文件,"r" = 只读
- `encoding="utf-8"`:指定编码,读中文必须加,否则乱码
- `with ... as f:` 是上下文管理器:用完自动关闭文件,不用手动 `f.close()`,防止资源泄漏

### 5. if __name__ == "__main__"
- 每个文件运行时都有内置变量 `__name__`
- 直接运行该文件时:`__name__ == "__main__"`
- 被 import 时:不是 `"__main__"`
- 所以入口代码放 if 里,模块被 import 时才不会产生副作用

### 6. 编码与乱码(实战踩坑)
- 文件在磁盘上是一串字节,编码决定了「字节 ↔ 字符」怎么转换
- 中文常用编码:UTF-8(现代标准)、GBK(Windows 旧默认)
- 读文件:open(path, encoding="utf-8") 告诉 Python 按 UTF-8 解码
- 写终端:Windows 的 Python 默认按系统编码(GBK)输出,而终端按 UTF-8 显示 → 乱码
- 修复:`sys.stdout.reconfigure(encoding="utf-8")` 强制输出 UTF-8
- 规律:**乱码 = 读/写用的编码和显示用的编码不一致**

## 第 2 课:解析简历为结构化数据

### 1. split() 切字符串
- 按指定字符把字符串切成列表
- `"a,b,c".split(",")` → `["a", "b", "c"]`
- `text.split("\n")` 按换行符切 → 一行行的列表
- 生活例子:按葡萄梗把一串葡萄掰成一颗颗

### 2. strip() 去首尾空白
- 去掉字符串开头和结尾的空格、换行等看不见的字符
- `"  专业技能  ".strip()` → `"专业技能"`
- 读文件时每行末尾都有换行符 `\n`,strip() 顺便清掉
- 生活例子:撕掉快递箱外面的胶带

### 3. 列表 list
- 一排东西,从 0 开始编号
- 用方括号 `[]` 创建:`names = ["张三", "李四", "王五"]`
- `names[0]` → `"张三"`(第 0 号抽屉)
- `len(names)` → `3`(几个抽屉)
- `names.append("赵六")` → 在末尾追加一个(往抽屉里加一件衣服)

### 4. for 循环
- `for 变量 in 列表:` — 对列表里的每一个元素,执行一遍缩进里的代码
- 循环次数 = 列表里有多少个元素
- 生活例子:收银员对着一排商品挨个扫码

### 5. startswith() 判断开头
- `"专业技能".startswith("专业")` → `True`
- `"专业技能".startswith("技能")` → `False`(开头不是"技能")

### 6. continue 跳过本次循环
- 在 for 循环里遇到 `continue`,直接跳到下一轮,不执行后面的代码
- 我们用它跳过空行:`if clean == "": continue`

### 7. 字典 dict
- 按名字(键)存取值,不是按编号
- 用花括号 `{}` 创建:`info = {"姓名": "郭园", "电话": "150..."}`
- `info["姓名"]` → `"郭园"`(用方括号取值)
- `info["新标签"] = "新内容"`(往字典里加新对)
- `len(info)` → 有几对
- `info.items()` → 返回 (键, 值) 的配对,常用于 for 循环
- 生活例子:新华字典,按字查释义

### 8. 数据类 dataclass
- 固定格式的数据容器,字段名和类型都预先定好
- `@dataclass` 装饰器告诉 Python 自动生成样板代码
- 用 `.字段名` 取值(不是方括号):`resume.skills`
- 生活例子:体检报告单,栏位固定,不能乱写
- vs dict:dict 是活页笔记本(随便加标签),dataclass 是固定栏位表格

### 9. `**` 拆字典传参
- `**sections` 把字典拆成 `键=值` 的形式传给函数
- `Resume(**{"header": [...], "skills": [...]})` 等价于 `Resume(header=[...], skills=[...])`
- 省得一个一个写参数名

### 10. 下划线开头的变量 `_xxx`
- Python 的约定:下划线开头表示"这是模块内部用的,外部不要直接碰"
- 不是强制规则,是给读代码的人看的信号

## 第 3 课:关键词匹配职位 JD

### 1. `in` 运算符 — 检查"在不在里面"
- `"Python" in "我熟悉 Python 和 SQL"` → `True`（子串出现在字符串里）
- `"Java" in "我熟悉 Python 和 SQL"` → `False`（没出现）
- `3 in [1, 2, 3, 4]` → `True`（元素在列表里）
- 生活例子:你在超市找"可乐"，从头走到尾，看到了就是"有"，走完没看到就是"没有"

### 2. `str.lower()` / `str.upper()` — 统一大小写
- `"Python".lower()` → `"python"`；`"PYTHON".lower()` → `"python"`
- 为什么需要:JD 写"python"，简历写"Python"，直接 `in` 对不上。都变小写再比
- 生活例子:两个人名字一个写"ZHANG Wei"一个写"zhang wei"，都转小写才能确认是同一个人
- 配套方法:`str.upper()` 是反向操作，全变大写

### 3. `if` / `else` 条件分支
- 结构:
  ```python
  if 条件:
      条件成立时走这里
  else:
      条件不成立时走这里
  ```
- `条件` 是一个能算出 `True` / `False` 的表达式（比如 `"Python" in text`）
- 冒号不能少，缩进不能错
- 生活例子:下雨(条件成立) → 带伞，不下雨(条件不成立) → 不带

### 4. 循环 + 判断 + 计数 — 批量检查模式
- 遍历关键词列表 → 逐个判断是否在简历里 → 分别装入命中/缺失列表
- 这是个很常用的模式:遍历 + 判断 + 分类收集
- 最后用 `命中数 / 总数` 算出匹配率

### 5. 浮点数与百分比格式化
- `len(hit) / len(keywords)` 得到的是浮点数（小数），比如 `0.666...`
- `f"{rate:.0%}"` :`.0%` 的意思是"乘以 100 显示，保留 0 位小数"，`0.666...` → `"67%"`
- 如果 `.1%` 就是保留 1 位小数:`0.666...` → `"66.7%"`

### 6. dict 返回多个结果
- 一个函数如果同时需要返回「每个词的结果 + 命中列表 + 缺失列表 + 匹配率」怎么办？
- 把多项数据包进一个 dict，一次 `return` 全带走
- 调用方用 `result["hit"]`、`result["rate"]` 取值
- 生活例子:体检完拿到一个报告袋，里面有血常规单、尿检单、心电图，各自按标签取

## 第 4 课:质量检查器

### 1. try/except — 异常处理的"安全网"

- 结构:
  ```python
  try:
      可能出错的代码
  except Exception as e:
      出错后的补救代码
  ```
- 放在 try 里的代码如果抛异常(报错),程序不会崩溃,而是跳到 except 继续执行
- `Exception` 是 Python 里所有异常的"祖宗",`except Exception` 能兜住几乎所有错误
- `as e` 把异常对象存到变量 `e`,`str(e)` 可以看到错误信息
- **本课的应用**:每条检查规则外面包 try/except,保证就算数据异常(比如 header 是空的、字段名写错了),检查器本身不会崩溃,而是返回一条"检查失败"的结果
- 生活例子:出门前看天气预报 — try(尝试出门),如果下雨(except),就带伞。你不会因为没看天气就被雨淋

### 2. any() — "至少有一个 True 就返回 True"

- `any([True, False, False])` → `True`(至少有一个 True)
- `any([False, False, False])` → `False`(一个 True 都没有)
- `any(char.isdigit() for char in text)` 是常见写法:遍历每个字符,只要有一个是数字就返回 True
- 生活例子:去超市买东西,购物清单上只要有一件能在超市买到,这趟就没白跑
- 配套函数:`all()` — 所有都是 True 才返回 True(一个都不能少)

### 3. enumerate() — 边遍历边计数

- `for i, line in enumerate(lines)`:同时拿到「第几个」和「内容」
- `i` 从 0 开始,要显示"第几行"时用 `i + 1`
- 比手动维护 `i = 0; i += 1` 更干净,不容易写错
- 生活例子:体育课排队报数,教官同时知道"你是第几个"和"你是谁"

### 4. getattr() — 用字符串动态取属性

- `resume.skills` 等价于 `getattr(resume, "skills")`
- 区别:`resume.skills` 的字段名是写死的,`getattr(resume, "skills")` 的字段名是字符串变量
- 什么时候用:当你需要循环遍历多个字段名时(比如用一个字段名列表 for 循环)
- 生活例子:`resume.skills` 是直接说"我要看技能栏",`getattr(resume, "skills")` 是拿着一张纸条上面写着"技能",让程序按纸条去找

### 5. sum() + 生成器表达式 — 一行统计

- `sum(1 for r in results if r["pass"])`:对每个满足条件的元素"加 1",得到符合条件的总数
- 生成器表达式 `(表达式 for 变量 in 可迭代对象 if 条件)` 和列表推导式很像,但用小括号不是方括号,它是惰性的(一个个生成而不是一次性全算出来)
- 生活例子:班主任数"考及格的有几个",每看到一个及格的同学就拨一颗算盘珠子,最后数有多少颗珠子

### 6. 踩坑记录:逐行搜索 vs 整段搜索(实战踩坑,面试好素材)

**背景**:练习给自己加的检查规则 `check_education_quality()`,想检查教育经历里有没有提到学校/学历。

**我犯的错**:对 `education` 的每一行分别搜关键词,认为每行都应该有"学校名 + 专业 + 学位":

```python
# ❌ 错误写法:逐行搜,要求每一行都有全部信息
for line in education:
    if "学校" not in line and "大学" not in line:
        missing_info.append("学校名称")
    if "专业" not in line:
        missing_info.append("专业")
    if "学位" not in line:
        missing_info.append("学位")
```

**为什么错**:
1. **搜索范围不对**:教育经历的信息是分散在多行里的——第一行写学校名("郑州财经学院"),其他行写专业/学历。信息是"分布在多行、合起来才完整"的,我却要求"每一行都包含全部信息",结果每一行都被误判成"缺失"。
2. **关键词漏了**:检查了 `"学校"` 和 `"大学"`,但"郑州财经**学院**"里是"学院",没被搜到。
3. **结果**:明明教育经历内容正常,却报告"缺少专业、学位、学校名称"。

**正确的思路**(参考 `check_contact_info` 的写法):先把所有行拼成一段文字,再在整段里搜关键词。

```python
# ✅ 正确写法:先拼成整段,再搜索
full_text = "".join(education)
edu_keywords = ["大学", "学院", "本科", "硕士", "专科"]
found = []
for kw in edu_keywords:
    if kw in full_text:
        found.append(kw)
```

**教训(一句话)**:写检查逻辑前,先想清楚"我要在什么范围里搜什么"。这个检查的范围是"整个教育经历段落",不是"每一行"。

**面试怎么讲**(把坑变成亮点):"我给自己加检查规则时犯过一个错——对教育经历逐行搜索,要求每行都有学校名+专业+学位,结果'郑州财经学院'因为只包含'学院'被误判为缺失。我意识到检查范围应该是整个段落,于是改成先把所有行拼接成一段再搜索。这个经历让我理解了:设计检查规则时,搜索范围要想清楚,信息是分布式的还是集中式的。"

## 第 5 课:接入 LLM 大模型,AI 重写简历

### 1. HTTP API 调用 — 你的程序「给别人的服务器发消息」

- 之前所有代码都在**自己电脑上**运行:读文件、切字符串、检查长度
- API 调用 = 你的程序通过网络给 DeepSeek 的服务器发一条消息,服务器算完把结果发回来
- 流程:`你的程序 →(发请求)→ DeepSeek 服务器 →(返回结果)→ 你的程序`
- `openai` 库帮我们做了「发消息、等回复、解析回复」的脏活,我们只需要调一个函数
- 生活例子:之前的代码是你自己在家里算账(本地计算);API 调用是你打电话给会计师请他帮忙算(远程计算)

### 2. 环境变量 .env — 密码不能写进代码

- **问题**:API Key 是密码,直接写进代码里 → git push 到 GitHub → 全世界都能看到 → 别人盗用你的额度
- **方案**:把密码存在单独的 `.env` 文件里,然后把 `.env` 加入 `.gitignore`(git 会自动忽略它,永不上传)
- `from dotenv import load_dotenv` + `load_dotenv()` — 程序启动时悄悄读 `.env`,把里面的值变成 Python 能用的变量
- `os.getenv("DEEPSEEK_API_KEY")` — 从环境变量里取出 key 的值
- `.gitignore` 文件 — 告诉 git「这些文件/文件夹不要管」,`.venv/`、`.env`、`__pycache__/` 都不该上传
- 生活例子:`.env` 是你家钥匙,放在口袋里不给人看;代码是你在外面说的话,别人能听到。钥匙绝不能从嘴里说出来

### 3. Chat Completion API — 对话补全模式

- DeepSeek(和 OpenAI)的核心 API 叫「Chat Completion」:你给一组消息,AI 补全后面的内容
- `messages` 是一个列表,每个元素是一个字典,有两个关键字段:
  - `role`:"角色",常用的是 `"system"`(设定 AI 人设)和 `"user"`(你的问题)
  - `content`:"内容",具体的文字
- `system` 角色:告诉 AI「你是谁」— 比如"你是一位简历优化专家"。这影响 AI 回答的语气和专业度
- `user` 角色:你具体问的问题,比如"请帮我优化简历"
- 生活例子:system = 你给演员的剧本角色设定("你演一个医生");user = 观众对演员说的话("医生我头疼")

### 4. 关键参数

- `model`:用哪个模型。`deepseek-v4-flash`(快/便宜,日常用)、`deepseek-v4-pro`(质量更高,重写简历用)
- `temperature`:创意程度,0~1。0 = 死板/确定性强,1 = 脑洞大/有创意。简历优化用 0.7 比较合适(有一定灵活性但不离谱)
- `max_tokens`:限制 AI 回复的最长长度。不加限制可能烧很多额度
- 生活例子:model = 你请哪个师傅(年轻便宜 vs 资深贵);temperature = 师傅按手册来还是自由发挥;max_tokens = "别写超过2000字"

### 5. Prompt 工程基础 — 把已有的分析结果喂给 AI

- **这是本课最重要的设计思路**:前 4 课的数据 → 拼成一段结构化的 prompt → 发给 AI → 得到有针对性的建议
- `build_prompt()` 函数做三件事:
  1. 把第 3 课的匹配结果摘要(命中哪些、缺失哪些、匹配率)
  2. 把第 4 课的检查结果摘要(哪些通过、哪些不通过)
  3. 加上简历原文 + 具体的改写任务要求
- 为什么这样做:如果不给 AI 这些上下文,AI 只能泛泛而谈。给了具体的缺失关键词和检查结果,AI 就能逐条给针对性建议
- 生活例子:去看医生,如果只说"我不舒服",医生的建议会很泛;如果你带了体检报告(相当于匹配结果 + 检查结果),医生就能针对你的具体指标开药

### 6. openai 库的基本用法

```python
from openai import OpenAI

# 创建客户端,指定 API Key 和服务器地址
client = OpenAI(api_key="sk-xxx", base_url="https://api.deepseek.com")

# 发请求
response = client.chat.completions.create(
    model="deepseek-v4-flash",
    messages=[
        {"role": "system", "content": "你是简历专家"},
        {"role": "user", "content": "请帮我改简历..."},
    ],
    temperature=0.7,
    max_tokens=2000,
)

# 取出 AI 的回复
advice = response.choices[0].message.content
```

- `response` 是服务器返回的完整响应对象,里面有多个选择(choices)
- `choices[0]` 取第一个(通常只有一个)选择
- `.message.content` 就是 AI 回复的文本

### 7. 整个数据流(第 1~5 课串联)

```
简历文件(.txt)
    │
    ├──→ read_text()           [第1课] 读成文本
    │
    ├──→ parse_resume()        [第2课] 解析成 Resume 对象 → 打印 5 段
    │
    ├──→ match_keywords()      [第3课] 关键词匹配 → 命中/缺失/匹配率
    │
    ├──→ check_all()           [第4课]  
    量检查 → 通过/不通过列表
    │
    └──→ get_optimization_advice()  [第5课]
            │
            ├── 拼 prompt: 简历原文 + 匹配结果 + 检查结果 + 任务要求
            ├── 调 DeepSeek API
            └── 返回优化建议 → 打印
```

### 8. 本课新增的文件

| 文件 | 作用 |
|------|------|
| `.env` | 存 API Key 和 Base URL,不进入版本控制 |
| `.gitignore` | 告诉 git 忽略 `.env`、`.venv/`、`__pycache__/` |
| `resume_optimizer/llm.py` | LLM 调用模块:建客户端、拼 prompt、调 API |

### 9. 练习(自己做,做完自己对答案)

1. 打开你的 DeepSeek 后台(platform.deepseek.com),看看这次调用消耗了多少 tokens。每个请求的用量在 API 日志里都能看到。
2. 把 `llm.py` 里的 `model="deepseek-v4-flash"` 改成 `model="deepseek-v4-pro"`,重新跑一次 `main.py`,对比两个模型给出的建议有什么不同。
3. 修改 `build_prompt()` 里的"任务要求"部分,加一条你自己的要求(比如"请特别关注教育经历的改写"),重新跑一次看效果。

## 第 6 课:Streamlit 网页界面

### 1. Web 应用 vs 命令行程序 — 本质区别

- 命令行程序:跑完就结束,结果打印在终端里,用户必须会敲命令
- Web 应用:服务器一直跑着,用户用浏览器访问,点了才执行
- 生活例子:命令行是"自己在家算账,算完笔记本合上";Web 应用是"开了一家自助查询店,顾客随时进来查"

### 2. Streamlit 是什么、为什么不用 HTML/CSS/JS

- 普通 Web 开发:HTML(结构) + CSS(样式) + JavaScript(交互),三个语言,入门门槛高
- Streamlit:全部用 Python 一个语言写,它自动渲染成网页
- 一个 `.py` 文件 = 一个网页;写一行 Python = 网页上多一块内容
- `streamlit run app.py` 启动一个本地服务器,浏览器打开 `http://localhost:8501` 就能看到页面
- `localhost` = "我自己的电脑",`8501` = 端口号(相当于房间号)

### 3. Streamlit 执行模型(面试常问!)

- Streamlit 脚本**从上到下重跑**
- 用户每次操作(点按钮、输入文字、上传文件),整个脚本从头执行一遍
- 这和命令行程序"执行完就退出"完全不同 —— 网页是"一直循环跑"的
- 代码文件一改,浏览器页面**自动刷新**

### 4. 本课用到的 Streamlit 组件

| 组件 | 代码写法 | 作用 |
|------|----------|------|
| 侧边栏 | `st.sidebar.xxx(...)` | 把组件放进左侧竖栏 |
| 标题 | `st.title("文字")` | 页面顶部大标题 |
| 副标题/说明 | `st.caption("文字")` | 标题下方的灰色小字 |
| 文件上传 | `st.file_uploader("提示", type=["txt"])` | 用户选文件上传,返回文件对象 |
| 文本输入 | `st.text_input("提示", placeholder="...")` | 单行输入框 |
| 下拉框 | `st.selectbox("提示", ["选项A","选项B"])` | 下拉选择 |
| 按钮 | `st.button("文字", type="primary")` | 点了返回 True |
| 进度条 | `st.progress(0.67, text="匹配率 67%")` | 显示一个进度条 |
| 转圈等待 | `with st.spinner("文字"):` | 包住慢操作,显示转圈动画 |
| 绿提示 | `st.success("文字")` | 绿色提示条 |
| 红提示 | `st.error("文字")` | 红色提示条 |
| 蓝提示 | `st.info("文字")` | 蓝色提示条 |
| Markdown | `st.markdown(文字)` | 渲染 Markdown 格式(AI 建议用它显示) |

### 5. 文件上传 — 和本地读文件的关键区别

- **第 1 课读磁盘文件**:有路径 → `open(path).read()` → 拿文本
- **网页上传文件**:文件在**内存**里,没有路径 → `uploaded.read().decode("utf-8")` → 拿文本
- 核心:上传的是"文件对象"(Streamlit 的 `UploadedFile` 类型),不是一个路径字符串
- `.read()` 读出来是**字节**(bytes),`.decode("utf-8")` 把字节转成字符串

### 6. `st.button` 的控制流模式

- 按钮是一个"开关":点了返回 `True`,没点返回 `False`
- 常见模式:`if st.button("开始分析"): 跑分析逻辑`
- 没点的时候只显示提示,点了才执行后面的代码,避免每次页面重跑都调 API

### 7. 已有模块一行不改 — 这也是一个面试点

- 第 1~5 课的 `parser.py`、`matcher.py`、`checker.py`、`llm.py` **完全没有动**
- 网页只是换了"外壳":把它们的**返回值**用 `st.success/error/markdown` 显示出来
- 这就是"模块化设计"的价值 —— 换界面不动内核

### 8. 踩坑:AI 回复被截断(finish_reason)

**现象**:网页上 AI 建议写到一半戛然而止,体验很差。

**原因**:`llm.py` 里 `max_tokens=2000`,但 prompt 里给 AI 提了 5 条任务要求,2000 token 写不完就被硬截。

**修复**:
1. `max_tokens` 从 2000 提到 **4096**(翻倍,5 条要求够写)
2. 调完 API 后检查 `response.choices[0].finish_reason`:
   - `"stop"` → AI 自然说完了(正常)
   - `"length"` → 被 max_tokens 卡断(追加一句红字警告,让用户知道被截了而不是内容就这些)
3. 把这个检查写进代码,以后即使再截也不会让用户一头雾水

**面试怎么讲**:"我做过一个优化:发现 DeepSeek API 调用有时被 max_tokens 硬截断,用户看到的是不完整的建议。我排查后做了两件事:调大了 max_tokens 让一般请求够用;同时在代码里检查 `finish_reason`,如果是 `length` 就在输出末尾追加一行可见的警告,让用户知道'这是被截断的,不是内容就这些'。这个小改进显著提升了用户体验,也是 API 调用里容易忽略的细节。"

### 9. 本课新增的文件

| 文件 | 作用 |
|------|------|
| `app.py` | 网页入口:Streamlit UI,搭页面、接模块、显示结果 |

### 10. 整个项目的最终数据流(第 1~6 课完整版)

```
浏览器(用户)
    │
    ├── 左侧边栏:上传 .txt、填关键词、选模型
    ├── 点「开始分析」
    │
    ▼
app.py(第6课,网页)
    │
    ├── uploaded.read().decode()    — 拿到简历文本
    ├── parse_resume(text)          — [第2课] → Resume 对象
    ├── match_keywords(text, kws)   — [第3课] → 匹配结果
    ├── check_all(resume)           — [第4课] → 检查结果
    ├── st.success/error/progress   — 显示匹配 + 检查
    │
    └── get_optimization_advice()   — [第5课] → AI 建议
            │
            ├── 拼 prompt(简历 + 匹配摘要 + 检查摘要)
            ├── 调 DeepSeek API
            ├── 检查 finish_reason 防截断
            └── st.markdown() → 显示在网页上
```

## 第 7 课:工程化收尾(轻量版:测试 + 配置 + README)

> 选择"轻量工程化":项目还小,只做最有价值的 3 样,日志跳过。

### 1. assert — 测试的地基

- `assert 条件` 是 Python 自带的关键词:条件为 `True`,安静放行;条件为 `False`,立刻报错停下
- 生活例子:过安检,身份证=本人,放行;不对,拦下报警
- 测试的本质 = 把对函数的"期望"写成一堆 assert
- 运行测试:`.venv/Scripts/python -m pytest tests`

### 2. pytest — 让测试自动跑的工具

- 约定:测试放 `tests/` 文件夹,文件名 `test_*.py`,函数名 `test_*`
- pytest 自动发现这些文件/函数,逐个运行,最后报告:几个通过、几个失败、失败在哪个文件哪一行
- 为什么测试要"函数式"组织:每个测试函数独立,一个失败不影响其他

### 3. 测试的经典手法:出题

- 给函数"出题":构造**合格数据**(期望 pass=True)和**不合格数据**(期望 pass=False)
- 比如测联系方式检查:有邮箱+电话 → 放行;只有名字 → 拦截
- 测试数据要"自包含":用写死在测试文件里的小样例,不依赖真实简历(除专门测读文件的用例)

### 4. 踩坑纪律:测试失败,先判断是代码错还是测试错

**实际经历**:第一次跑测试 23 个里挂了 2 个,都是我写测试时自己犯的错:
1. `test_mixed_hit_and_miss`:传了 4 个关键词 3 个命中,匹配率是 3/4=0.75,我却断言 0.5(期望值算错)
2. `test_total_length_ok`:造的测试简历总字数不到 300,`check_total_length` 默认要求 300 字,正确地判"不通过",我却期望它通过(测试数据太短)

**教训**:看到测试红了,**第一反应不是去改产品代码,而是先问"是代码坏了,还是测试写错了?"** 这次是测试错、代码对——如果慌着改产品代码,反而会把对的改坏。

### 5. config.py — 集中配置(消灭"魔法数字")

- 魔法数字/魔法字符串:裸写死在代码里的值(比如 `temperature=0.7`),没名字、没人知道是干嘛的
- config.py 把"可调参数"集中成有名字的常量:模型列表、默认模型、温度、max_tokens、默认路径、默认关键词
- 其他文件 `from config import xxx` 引用,改一处全局生效
- config.py 只有常量没有逻辑,是"参数总表";生活例子:配电箱,所有开关标好名字集中一处
- 注意:config.py 放根目录,运行时也必须在根目录执行(python main.py / streamlit run app.py 都满足)

### 6. README.md — 项目门面

- README 是 GitHub 仓库第一眼看到的东西,面试官筛项目先看它(是不是有个像样的说明)
- 没有 README 的项目 = 面试官点开就划走
- 结构:一句话介绍 → 功能列表 → 快速开始(3 步) → 怎么跑测试 → 项目结构 → 技术栈 → 配置说明
- 配套 `.env.example`(API Key 模板)和 `requirements.txt`(依赖清单)

### 7. 第 7 课新增的文件

| 文件 | 作用 |
|------|------|
| `tests/test_parser.py` | 测解析:parse_resume / read_text |
| `tests/test_matcher.py` | 测匹配:match_keywords |
| `tests/test_checker.py` | 测检查:9 条规则 + check_all |
| `config.py` | 集中配置:所有可调参数 |
| `README.md` | 项目门面 |
| `.env.example` | API Key 模板(不提交真实 key) |
| `requirements.txt` | 依赖清单 |

### 8. 为什么不测 llm.py?(测试边界)

- llm.py 要调 DeepSeek 真实 API:测试不该依赖网络、不该花钱、不该要求 API Key 存在
- 只测"稳定、纯逻辑、不花钱"的部分 —— 这也是工程里的测试边界意识

## 第 8 课:AI 自动提取 JD 关键词(结构化输出)

### 1. 结构化输出 — 让 AI 返回"程序能用"的数据

- 第 5 课 AI 返回的是给人看的文本(建议);第 8 课要的是程序能直接用的数据(关键词列表)
- 做法:prompt 里明确要求"只返回 JSON 数组,形如 [\"Python\", \"API\"]",不给解释文字
- 生活例子:第 5 课是厨师口述菜谱(人看),第 8 课是机器打出购物清单(程序用)

### 2. json 模块 — 把 JSON 字符串变成 Python 数据

- `json.loads('["Python", "API"]')` → `["Python", "API"]`
- JSON 和 Python 数据结构几乎一一对应:`[...]` → 列表,`{...}` → 字典
- AI 返回的是"长得像 JSON 的字符串",`json.loads` 负责把它真正变成列表

### 3. 正则 re — 在文字里"抠"出想要的部分

- `re.search(r"\[.*\]", text, re.DOTALL)`:找第一个方括号包裹的部分
- `re.DOTALL`:让 `.` 也能匹配换行(默认 `.` 不匹配换行,AI 回复可能多行)
- 生活例子:在整页报纸里,用"找第几版第几栏"的规则把某段新闻抠出来

### 4. 解析要容错(AI 不一定老实)

AI 可能:包 markdown 代码块 ```` ```json [...] ``` ````、前后夹解释文字。
`parse_keyword_json` 的容错顺序:
1. 先直接 `json.loads` 整段 —— 最干净的情况
2. 失败 → 正则抠出 `[...]` 再 `json.loads`
3. 还失败 → 抛 ValueError(让上层给用户看明白的提示,而不是静默返回垃圾)

### 5. 温度用低值(提取和改写不一样)

- `get_optimization_advice` 用 `temperature=0.7`(改写要有创意)
- `extract_keywords_from_jd` 用 `temperature=0.3`(提取要稳定、可复现)
- 规律:任务越需要"准确/稳定",温度越低;越需要"创意/发散",温度越高

### 6. 设计:拆成"调 AI"和"解析"两个函数(复用第 7 课测试边界)

```
extract_keywords_from_jd(jd_text)  # ① 调 AI 拿原始回复 —— 依赖网络/花钱,不测
        │
        ▼
parse_keyword_json(raw_text)       # ② 把回复解析成列表 —— 纯逻辑,可测
        │
        ▼
["Python", "API"]                  # 交给 match_keywords
```

- 好处:解析逻辑能写测试(tests/test_llm.py 有 7 个用例),调 AI 的部分不测
- 这就是"把不稳定的外部依赖和稳定的纯逻辑分开"的设计思想

### 7. 本课新知识汇总

| 新知识 | 说明 |
|--------|------|
| 结构化输出 | 让 AI 返回 JSON,程序能直接解析 |
| `import json` + `json.loads()` | JSON 字符串 → Python 数据 |
| `import re` + `re.search()` | 正则搜索,`re.DOTALL` 让 . 匹配换行 |
| `isinstance(x, list)` | 检查变量是不是列表 |
| `raise ValueError("...")` | 手动抛异常,给调用方明确错误 |
| `except (A, B)` | 一次捕获多种异常 |
| `st.multiselect` | 网页多选框(让用户勾选保留哪些关键词) |

### 8. 本课新增/修改的文件

| 文件 | 改动 |
|------|------|
| `resume_optimizer/llm.py` | 新增 `extract_keywords_from_jd()` + `parse_keyword_json()` |
| `app.py` | 侧边栏加 JD 上传/粘贴,AI 自动提取关键词 + 多选确认,手动关键词兜底 |
| `tests/test_llm.py` | 新增 7 个测试(只测 parse_keyword_json 纯函数) |

## 第 9 课:导出优化版简历(文件写入)

### 1. 文件写入 —— 和第 1 课读取正好相反

| | 方向 | 用法 |
|---|------|------|
| 读取(第 1 课) | 文件 → 字符串 | `open(path, "r")` |
| 写入(第 9 课) | 字符串 → 文件 | `open(path, "w")` |

- `"w"` = write:文件不存在会**新建**,已存在会**整个覆盖**(重跑一次就是新内容)
- `with open(...) as f:` 用完自动关闭,和读一个道理
- `encoding="utf-8"`:写中文必须指定,否则按系统编码写会乱码(第 1 课读的坑,写照样有)
- 文件夹不存在时 `open()` 会报错 → 先用 `os.makedirs()` 建出来

### 2. os 路径处理(配合写入)

- `os.path.dirname(path)` → 取文件夹部分:`"output/optimized_resume.txt"` → `"output"`
- `os.path.exists(path)` → 判断路径存不存在
- `os.makedirs(path)` → 创建文件夹(不存在的上级会一起建)

### 3. 抽公共函数 `_summarize_analysis`(工程习惯:DRY)

- `build_prompt`(第 5 课)和 `build_rewrite_prompt`(第 9 课)都要把"匹配+检查结果"压成摘要
- 写两遍容易**改一处忘另一处** → 抽成公共函数,改一处全局生效
- 函数名前加 `_` 表示"内部函数,别的文件别 import 我"——Python 约定

### 4. 关键设计:AI 输出格式要和下游工具兼容

- 导出文件的目标之一:**能被本工具 `parse_resume()` 再读回来**,形成"改→查→再改"循环
- `parse_resume()` 靠 `SECTION_TITLES` 逐行**精确比对**切段,标题必须一字不差
- 所以 `build_rewrite_prompt` 要求 AI 保留四个标题、禁止加 markdown 标记

### 5. 实战踩坑(面试好素材):AI 给标题加了星号

- **现象**:第一次真机导出后,回读验证发现 header 27 行、其他四段全空
- **原因**:AI 把标题写成 `**专业技能**`(加了 markdown 粗体 `**`),而解析器比的是精确字符串 `"专业技能"`,不认 → 整段内容全掉进 header
- **修复**:prompt 第 2 条改成"标题只写纯文字,禁止 ** 星号、# 号等任何 markdown 标记",重跑后五段全部正确
- **规律**:光说"保留标题"不够,AI 会自作主张加装饰;prompt 要求越具体,输出越可控。和第 8 课"AI 不老实要做容错"是同一类问题,这次选择在源头(prompt)堵上

### 6. 验证:回读测试才是闭环打通

- 导出后用 `parse_resume()` 读回,检查五段都有内容——这比肉眼检查可靠
- 第一次回读:header 27 行、其余全空 → 有 bug
- 修复后再回读:五段齐全 → 通过

### 7. 网页版:`st.download_button`

- 数据在内存里(字符串),点一下浏览器直接下载,不用先存服务器磁盘
- 命令行版无选择框 → 重写用默认 `REWRITE_MODEL`(pro);网页版用户已选模型 → 跟着用户选走

### 8. 本课新知识汇总

| 新知识 | 说明 |
|--------|------|
| `open(path, "w")` | 写模式:不存在新建,存在覆盖 |
| `f.write(text)` | 把字符串写进文件 |
| `os.path.dirname` / `exists` / `makedirs` | 路径处理、判断、建文件夹 |
| 抽公共函数 | 重复代码合并成函数,前加 `_` 表示内部 |
| 输出格式兼容 | AI 输出要匹配下游解析器,才形成闭环 |
| `st.download_button` | 网页下载按钮 |

### 9. 本课新增/修改的文件

| 文件 | 改动 |
|------|------|
| `config.py` | 新增 `REWRITE_MODEL`(pro)/ `OUTPUT_PATH` 常量 |
| `resume_optimizer/llm.py` | 新增 `_summarize_analysis` / `build_rewrite_prompt` / `get_rewritten_resume`;`build_prompt` 改用公共函数 |
| `resume_optimizer/exporter.py` | **新增**,唯一函数 `save_resume`(文件写入) |
| `main.py` | 新增"导出优化版简历"环节 |
| `app.py` | 新增"下载优化版简历"按钮 |
| `tests/test_exporter.py` | **新增** 3 个测试(文件写入,不联网,用 tmp_path) |

## 第 10 课:RAG 概念 + 建知识库 + 切块(2026-08-08)

### 1. RAG 是什么:闭卷考试 → 开卷考试

- **现状**:第 5/9 课让 AI 改简历,AI 靠训练时记住的知识"凭本事猜"岗位要什么 = 闭卷考试
- **RAG = Retrieval-Augmented Generation = 检索增强生成**,拆开:
  - Retrieval 检索:先从知识库里找出和当前 JD 最相关的那几段
  - Augmented 增强:把这几段拼进 prompt 喂给 AI
  - Generation 生成:AI 参考真实岗位要求再改写
- **对简历优化价值**:知识库存 20 份真实 AI 岗 JD,AI 改写时参考真实要求,命中关键词不再靠蒙
- 核心一句话:**先翻书找到相关章节,再答题**(开卷 vs 闭卷)

### 2. 知识库 = 一摞真实岗位要求文档

- 放 `examples/knowledge/` 目录下,一个 .txt 一份岗位 JD
- 先放了 3 份**示例**文档(贴合 AI 应用开发方向)跑通框架
- 以后把 BOSS 上投的真实 JD 都丢进去,积累越多检索越准 = 自己的"岗位情报库"

### 3. 为什么切块?为什么按段落切?

- 一份 JD 几百字,整篇塞给 AI 又长又不准;切成小段,检索能精确到"最相关的那一小段"
- 按**空行分段**(不是按字数硬切):文档天然按空行分层(职责/要求/加分项),空行是"内容边界"
- 一行太长(超 chunk_size)时再按中文标点(，。、；)切,保留语义,不把句子腰斩
- 生活例子:乐高零件按类分装到小盒子,同类(同段)放一起

### 4. 新知识:列表切片、rfind、while 循环

| 新知识 | 说明 |
|--------|------|
| `os.listdir(path)` | 列出目录下所有文件名 |
| `os.path.isdir` / `join` / `abspath` | 判断目录/拼路径/转绝对路径 |
| 列表推导式 | `[x for x in ... if x.strip()]`,一行造新列表 + 过滤 |
| `str.rfind(子串, 起点, 终点)` | 从右往左找子串位置,找不到返回 -1 |
| while 循环 | 条件为真就一直跑,切块时"没切完就继续切" |
| 内部函数前缀 `_` | `_split_long_line` 表示"别从外部 import 我" |

### 5. 验证结果

- 3 份文档分别 638/575/490 字,切成 20/19/19 块,每块一个职责点,语义完整

### 6. 本课新增/修改的文件

| 文件 | 改动 |
|------|------|
| `examples/knowledge/01-ai-application-engineer.txt` | **新增**示例岗位 01 |
| `examples/knowledge/02-rag-engineer.txt` | **新增**示例岗位 02 |
| `examples/knowledge/03-python-backend-dev.txt` | **新增**示例岗位 03 |
| `resume_optimizer/knowledge.py` | **新增**:`read_knowledge_files()` 读目录 + `chunk_text()` 切块 |
| `tests/test_knowledge.py` | **新增**(2026-08-11 补):`chunk_text` 纯逻辑 7 个测试 |

## 第 11 课:向量检索 —— RAG 的"搜"(2026-08-11)

### 1. 核心问题:怎么让机器知道"这段文字和那段文字意思相近"?

答案:把文字变成一串数字(向量),然后比数字。

### 2. 向量 = 文字在数学空间里的"坐标"

- 就像地图上每个城市有经纬度;文字也有"含义坐标"——只不过不是 2 个数,而是几千个数
- 把文字转成向量的过程叫**向量化(embedding)**
- 本课用 **TF-IDF**(下面讲),以后学到更强的 embedding 模型可以替换

### 3. TF-IDF:词频 × 逆文档频率

- **TF(词频)**:这个词在这段文字里出现了几次
- **IDF(逆文档频率)**:整个知识库里有几篇提到了这个词(越少见越值钱)
- 思路:一个词在这段文字里出现多次,但在整个知识库里很少见 → 这个词就是这段文字的"特征词",权重高
- 最终每块文本转成一个向量:每个位置 = 一个词的 TF × IDF
- 本课用 `sklearn.feature_extraction.text.TfidfVectorizer` 完成向量化
- `analyzer="char"` + `ngram_range=(1,3)`:中文按字切,单字+双字+三字组合都算特征

### 4. 余弦相似度:两个向量"方向有多近"

- 两个向量夹角越小 → 方向越近 → 含义越相似
- **公式:cos(θ) = (A·B) / (|A| × |B|)**
  - 分子 A·B = 点积(对应位置相乘再求和)
  - 分母 |A| × |B| = 两个向量各自的长度(平方和开根号)相乘
- 范围 [-1, 1]:1 = 完全相同方向,0 = 垂直(无关),-1 = 完全相反
- 生活例子:两个人往同一个方向走(余弦≈1),各走各的(余弦≈0)

### 5. 检索流程

```
查询("Python 后端开发") → 向量化 → [0.1, 0.3, 0.0, ...]
                                        ↓
知识库每块也都向量化了        → 逐个算余弦相似度
                                        ↓
                           取相似度最高的 top_k 块 → 返回
```

`retrieve()` 函数三步:
1. 查询文本用同一套 vectorizer 转成向量
2. 跟索引里每一块向量逐个算余弦相似度
3. 按相似度降序排序,取前 top_k 个

### 6. 新知识汇总

| 新知识 | 说明 |
|--------|------|
| 向量 (vector) | 一串数字,代表文字的"含义坐标" |
| TF-IDF | 词频 × 逆文档频率,重要词权重高 |
| 余弦相似度 | 两个向量夹角,0~1,越大越相关 |
| 点积 (dot product) | 对应位置相乘再求和 |
| 向量长度 (L2 norm) | 各分量平方和开根号 |
| `zip(a, b)` | 把两个列表"拉链"配对,返回 (a[0],b[0]), (a[1],b[1])... |
| `math.sqrt(x)` | 开平方根 |
| `lambda pair: pair[1]` | 匿名函数,一行写完的小函数,常用作 sort 的 key |
| `pytest.approx(x)` | 浮点数近似比较,解决 `-0.9999999999999998 ≠ -1.0` 的精度问题 |
| `TfidfVectorizer` | sklearn 的 TF-IDF 向量化器,一行代码完成整批文本向量化 |

### 7. 踩坑:浮点数精度 —— -1.0 ≠ -1.0?

- **现象**:测试 `test_cosine_opposite` 失败,`cosine_similarity([1,2], [-1,-2])` 返回 `-0.9999999999999998`,不是 `-1.0`
- **原因**:浮点运算有微小误差,数学上 = -1,计算机算出来差 0.0000000000000002
- **修复**:`assert result == approx(-1.0)`,用 pytest 的 `approx` 做近似比较
- **规律**:涉及小数的比较,永远用 `approx` 或 `math.isclose`,不要用 `==`

### 8. 本课新增/修改的文件

| 文件 | 改动 |
|------|------|
| `resume_optimizer/retriever.py` | **新增**:`cosine_similarity` / `build_knowledge_index` / `retrieve` |
| `tests/test_retriever.py` | **新增**:9 个测试(纯数学函数) |
| `tests/test_knowledge.py` | **新增**:7 个测试(第 10 课漏的,今天补上) |
| `requirements.txt` | 新增 `scikit-learn` / `numpy` |

## 第 12 课:prompt 注入 —— RAG 的"增强"(2026-08-13)

### 1. 核心问题:搜到了参考材料,怎么喂给 AI?

上一课把"和 JD 最相关的岗位要求"**搜**出来了(检索),但搜出来不用 = 白搜。
本课把检索结果拼进给 AI 的 prompt,让 AI 改写时参考真实岗位要求——这就是 RAG 的"增强"。

### 2. 开卷考试 vs 闭卷考试

- 第 9 课:AI 只看到简历 + 分析摘要,凭自己脑补改 = **闭卷考试**
- 第 12 课:AI 先看到检索到的【岗位知识参考】再改 = **开卷考试**
- 生活例子:写论文时,一份凭记忆写,一份旁边放着参考资料写——后者更贴合要求、更少凭空编

### 3. 改动一:llm.py 开"参考材料"的口子

- `build_prompt` / `build_rewrite_prompt` 都加参数 `knowledge_context=""`(默认空串)
- 有值时,在 prompt 里拼一段【岗位知识参考】;空串 = 整段不出现,行为跟第 9 课完全一样
- **好处**:参数带默认值,老调用一行不改,新功能随时开——这就是"向后兼容"

### 4. 改动二:knowledge.py 一条龙检索

`build_knowledge_context(jd_text, top_k=3)`:读知识库 → 切块 → 建索引 → 检索最相关 top_k 块 → 拼成编号段落文字。返回空串 = 知识库空/没检索到,上层就当没带参考。

### 5. 改动三:入口接上

- `main.py`:读 JD → build_knowledge_context → 把参考传给两个 AI 函数;新增"岗位知识检索"打印环节
- `app.py`:有 JD 才检索(没 JD 只填手动关键词时跳过),新增"📚 AI 参考的岗位知识"折叠框,把参考传给两个 AI 函数

### 6. 踩坑:RAG 增强后输出变长,token 上限不够了

- **现象**:加参考后 AI 输出更长,4096(建议用)和 8192(改写用)双双被截断;改写简历最后一条"个人优势"断在"工具"二字,截断警告还**混进了导出的简历文件**
- **修复**:config.py 里 `MAX_TOKENS` 4096→8192,`REWRITE_MAX_TOKENS` 8192→16384
- **规律**:RAG 加内容 = prompt 变长 + 输出变长,别忘同步调大 max_tokens(输入变长还可能导致输出啰嗦,这也正常)

### 7. 新知识汇总

| 新知识 | 说明 |
|--------|------|
| prompt 注入 (注入) | 把检索到的相关内容拼进给 AI 的 prompt |
| RAG 全流程串通 | 检索 → **增强**(拼进 prompt) → 生成(AI 输出) |
| 可选参数 + 默认值 | 参数设默认值,新功能不破坏旧调用(向后兼容) |
| `finish_reason == "length"` | AI 因字数上限被截断,输出不完整,尾部会有警告行 |

### 8. 本课新增/修改的文件

| 文件 | 改动 |
|------|------|
| `resume_optimizer/llm.py` | 4 个函数加 `knowledge_context` 参数,prompt 有值时拼【岗位知识参考】 |
| `resume_optimizer/knowledge.py` | **新增** `build_knowledge_context()`:读→切→索引→检索→拼段落 |
| `main.py` | 新增"岗位知识检索"环节,把参考传给两个 AI 函数 |
| `app.py` | 有 JD 时检索 + 显示"AI 参考的岗位知识"折叠框 + 传参考 |
| `config.py` | `MAX_TOKENS` 4096→8192,`REWRITE_MAX_TOKENS` 8192→16384 |

### 9. 运行验证(2026-08-13,真实调 API)

- 岗位知识检索:从知识库检索到 3 块与 JD 最相关的岗位要求 ✅
- AI 优化建议:正常输出,不再被截断 ✅
- 导出改写简历:个人优势完整收尾,无截断警告 ✅
- 改写质量:融入参考要求(OpenAI 兼容接口/DeepSeek、RAG、MCP Server),缺失关键词补齐,STAR 结构 ✅

## 第 13 课:RAG 收尾 —— 测试 + 清单核对(2026-08-13)

### 1. 为什么收尾要补测试?

第 12 课改了代码但没写测试。测试是安全网——以后改坏了能第一时间发现。
本次补 5 个测试(49 → 54 全绿),把 RAG 的行为锁住。

### 2. 测试一:"贴纸条"行为(test_llm.py +3)

`build_rewrite_prompt` 是纯函数(字符串进 → 字符串出),不调 API、不花钱,最适合测试:
- 传了参考 → prompt 含【岗位知识参考】和内容
- 不传 → 不含(跟第 9 课一样)
- 传空串 → 等同没传

### 3. 测试二:"查书助手"(test_knowledge.py +2)+ 两个新技能

`build_knowledge_context` 默认读"真知识库"(examples/knowledge/),直接测依赖真实文件、不稳定。
解决:自己造一个"临时知识库"来测。用到 pytest 的两个内置道具:

- **`tmp_path`**:pytest 给每个测试发一个"专属空文件夹",测完自动清掉
- **`monkeypatch`**:测试时临时把函数里用的路径/变量**指到别处**,测完自动还原——不污染真数据

流程:在专属文件夹里放两份自写文档(一份 Python 后端、一份前端)→ monkeypatch 把知识库路径指过去 → 拿"找 Python 后端"的 JD 检索 top_k=1 → 断言搜到的是 Python 相关的、不是前端那份。

### 4. 踩坑:测试预期写得太具体

一开始断言"搜到【岗位名称】那一块",失败——因为 JD 里提了 FastAPI,"要求:熟悉 Python、FastAPI、MySQL"这块(也含 FastAPI)相似度更高,被选中了。**检索完全正常,是断言写死错了。**

教训:断言别猜"具体哪一块被选中",断言"行为正确"(相关的来了、无关的没来)。

### 5. 新知识汇总

| 新知识 | 说明 |
|--------|------|
| `tmp_path` | pytest 内置 fixture,每个测试一个临时目录,测完自动清理 |
| `monkeypatch` | 测试时临时替换函数/变量(如路径),测完自动还原,不污染真数据 |
| 测试预期要"稳" | 别断言"具体哪块",断言"行为正确"(相关的来了、无关的没来) |

### 6. 本课新增/修改的文件

| 文件 | 改动 |
|------|------|
| `tests/test_llm.py` | **+3**:build_rewrite_prompt 传参考/不传/传空串 → 是否含【岗位知识参考】 |
| `tests/test_knowledge.py` | **+2**:build_knowledge_context 临时知识库检索命中 / 空库返回空串 |
| `PROJECT.md` / `CAREER.md` | RAG 升级勾选完成、进度核对 |

RAG 升级到此全部完成:检索(第 11 课)→ 增强(第 12 课)→ 测试收尾(第 13 课),54 个测试全绿。
