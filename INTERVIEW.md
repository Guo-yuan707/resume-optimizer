# 面试问答集

> 每课把「面试官可能问的问题 + 参考答案」记在这里。要求:能用自己的话讲,而不是背。

## 第 1 课

**Q1: 介绍一下你的项目,它解决什么问题?**
A: 这是一个简历优化工具。用户输入自己的简历和想投的职位描述,工具能分析关键词匹配度、检查简历质量问题,并用大模型生成改写建议。整个项目我从零搭建,包括文件解析、规则引擎、LLM 调用和网页界面。

**Q2: 为什么用虚拟环境?**
A: 不同项目的第三方库版本可能冲突。venv 为每个项目隔离依赖,保证环境干净、可复现。团队协作或部署时也更可靠。

**Q3: 读文件为什么用 with open(),而不是直接 open()?**
A: 用 with 是「上下文管理器」,代码块结束后自动关闭文件句柄,避免资源泄漏。直接 open() 的话必须手动调用 f.close(),而且如果中间抛异常就关不掉了。

**Q4: if __name__ == "__main__" 是什么作用?
**
A: 直接运行该文件时 __name__ 等于 "__main__",才执行入口代码;被其他模块 import 时不会执行。这样我的模块既能单独运行,也能被复用,不会有副作用。

**Q5: 为什么读中文要写 encoding="utf-8"?**
A: 文本在磁盘上是字节序列,UTF-8 是中文常见的编码。不指定编码时,Windows 默认可能用 GBK,读 UTF-8 写的中文会乱码甚至报错。

**Q6: 你项目里遇到中文乱码是怎么解决的?**
A: 项目用 UTF-8 读文件,但 Windows 的 Python 往终端输出时默认按系统编码 GBK 写,终端按 UTF-8 显示,就乱码了。我在入口调了 `sys.stdout.reconfigure(encoding="utf-8")`,强制输出也用 UTF-8。核心规律是:乱码 = 读/写/显示三方的编码不一致。

**Q7: UTF-8 和 GBK 有什么区别?**
A: 都是把字符编码成字节的方案。UTF-8 是变长编码,中文一个字符通常占 3 字节,支持全世界所有文字,是现代标准;GBK 是中文专用编码,中文占 2 字节,主要在旧的 Windows 中文系统里用。跨系统传文件普遍用 UTF-8。

## 第 2 课

**Q1: 简历解析的核心思路是什么?**
A: 简历本质是"标题+内容"的半结构化文本。我用了两个线索来解析:① 空行分段(空行是段落边界);② 已知标题名单(提前告诉程序"专业技能""教育经历"等是标题)。程序逐行扫描,遇到标题就切换当前段落,普通行归入当前段落。不需要正则或 AI,纯字符串操作就够。

**Q2: list 和 dict 有什么区别?各自适合什么场景?**
A: list 是按位置索引的有序集合,用 `[0]` 取值,适合"按顺序处理一堆东西";dict 是按键(key)索引的无序集合,用 `["键名"]` 取值,适合"按名字查找"。简历解析里,每段内容行用 list 存(比如 skills 下面有多行),不同段用 dict 映射(标题名→对应内容列表)。

**Q3: dataclass 是什么?为什么要用它?**
A: dataclass 是 Python 3.7+ 的装饰器,自动给类生成 `__init__`、`__repr__` 等样板方法。相比 dict,dataclass 的字段名和类型是固定的——写错字段名 IDE 会提示,类型不匹配也能及早发现。适合做"数据载体",比如我的 Resume 类就有 header/skills/education/projects/strengths 五个固定字段。

**Q4: strip() 做了什么?为什么解析文本时常用?**
A: strip() 去掉字符串开头和结尾的所有空白字符(空格、换行、制表符等)。读文件时每行末尾都带 `\n`,strip() 一次清干净;简历里有时行首有缩进空格,strip() 也能去掉,让后续的标题匹配更可靠。

**Q5: for 循环里的 continue 是干什么的?**
A: continue 跳过当前这轮的剩余代码,直接进入下一轮循环。我的解析器里用它跳过空行:`if clean == "": continue`——空行没有内容要处理,直接看下一行。

**Q6: `**` 在 Python 函数调用里是什么意思?**
A: `**` 把字典拆成键值对传给函数。比如 `Resume(**{"header": [...], "skills": [...]})` 等价于 `Resume(header=[...], skills=[...])`。在解析器里省得我逐个字段手写,一行搞定。

**Q7: 如果简历格式变了(比如出现新的标题类型),你的解析器怎么扩展?**
A: 在 `SECTION_TITLES` 列表里加新标题名,同时在 `_TITLE_TO_FIELD` 字典里加上新标题到字段名的映射,再在 Resume dataclass 里加一个新字段即可。三处改动,都是声明式的,不用改解析逻辑。

## 第 3 课

**Q1: 关键词匹配是怎么做的?核心思路是什么?**

A: 把 JD 的关键词逐个放到简历全文里"搜"，用 Python 的 `in` 运算符检查子串是否出现。为了不因大小写漏掉，两边都先调 `.lower()` 统一变小写。最后统计命中数除以总数，得到匹配率。整个逻辑就是一个循环 + if/else 判断，不需要正则或复杂算法。

**Q2: 为什么要用 `.lower()` 统一变小写?不用会有什么问题?**

A: `in` 是精确匹配——`"Python" in "python工程师"` 返回 False，因为大小写不同。JD 里可能写 "python"，简历里写 "Python"，不统一就会把明明有的技能判成缺失，匹配率偏低。两边都 `.lower()` 之后，"python" in "python工程师" 就是 True 了。

**Q3: `in` 运算符和 `==` 有什么区别?**

A: `in` 检查"子串在不在字符串里"——只要出现就算，不管位置；`==` 检查"两个字符串完全相等"——必须一模一样。关键词匹配用 `in`，因为简历里是"我熟悉 Python 和 SQL"这样一句话，关键词是嵌入其中的。

**Q4: 匹配率是怎么算的?为什么要用浮点数?**

A: 命中数 ÷ 关键词总数。比如 8 ÷ 12 = 0.666...，是浮点数。用 `f"{rate:.0%}"` 格式化显示成 "67%"。用浮点数是因为整数除法（`//`）会丢掉小数，8 // 12 = 0，没法展示百分比。

**Q5: 如果 JD 关键词在简历里以不同形式出现（比如"向量数据库"和"向量化"），能匹配上吗?**

A: 当前用 `in` 做子串匹配，能部分覆盖——比如关键词写"向量"能匹配到"向量化"和"向量数据库"。但反过来，关键词写"向量数据库"，简历只写了"向量化算法"，就匹配不上（因为"向量数据库"这整个字符串没出现）。后续升级方向是用正则或 NLP 做语义级别匹配。

**Q6: 函数要返回多个值怎么办?为什么用 dict 而不是 return 多个值?**

A: Python 虽然可以 `return a, b, c`（实际上返回元组），但调用方得记住顺序，容易写错。用 dict 包装后，调用方按名字取——`result["hit"]`、`result["rate"]`——一目了然。这也方便以后加新字段，不改调用方代码。

## 第 4 课

**Q1: 你的简历质量检查器做了哪些检查?**

A: 一共 5 类规则、8 条检查：
① 联系方式检查 — header 里有没有 @（邮箱）和数字（电话）；
② 各段落长度检查（4 段）— 技能/教育/项目/个人优势各自是否至少 2 行；
③ 空项检查（2 段）— 技能和项目里有没有"只写了项目符号但没内容"的行；
④ 整体长度检查 — 总字符数是否够 300 字；
⑤ 每条规则都包在 try/except 里，数据异常时不会崩溃而是返回错误信息。

**Q2: 为什么每条检查规则都包了 try/except？不包会怎样？**

A: 检查器本质上是在"读数据"，数据可能不正常——比如 header 是空的、字段名拼错了。如果不包 try/except，一条规则崩溃了，整个 check_all() 就中断了，后面的规则都跑不了。包了之后，即使某条规则出错，也会返回一条带错误说明的结果，不影响其他规则继续检查。这是一种防御性编程：对外部数据永远不要假设它是完美的。

**Q3: try/except 是怎么工作的？Exception 是什么？**

A: Python 在运行出错时"抛出异常"（throw）——比如 `header[0]` 在 header 为空时抛出 `IndexError`。如果抛异常的代码在 try 块里，Python 会跳到 except 块执行处理代码，程序本身不会崩溃。`Exception` 是 Python 里几乎所有异常的父类，`except Exception` 能兜住绝大部分错误。`as e` 让你拿到异常对象，`str(e)` 可以看到错误信息。

**Q4: `any()` 是干什么的？你的代码里怎么用的？**

A: `any()` 检查一个可迭代对象里是否"至少有一个 True"。我在联系方式检查里用 `any(char.isdigit() for char in full_header)` 判断 header 里有没有数字——只要 header 文本里有一个数字字符，就说"有电话号码"。这种写法比写 for 循环 + flag 变量更简洁。

**Q5: `enumerate()` 是什么？什么时候用它？**

A: `enumerate()` 在遍历列表时同时给你「索引」和「元素」——`for i, line in enumerate(lines)` 让你同时知道"这是第几个"和"内容是什么"。索引从 0 开始。我在空项检查里用它，告诉用户"第几行看起来是空的"，便于定位问题。比手动 `i = 0` 然后每次 `i += 1` 更干净。

**Q6: `getattr()` 是什么？为什么不用 `resume.skills` 直接取？**

A: `getattr(对象, "属性名字符串")` 用字符串动态取属性。`resume.skills` 和 `getattr(resume, "skills")` 结果一样，但前者字段名写死了，后者的字段名可以是变量。我在整体长度检查里用一个字段名列表 `["header", "skills", ...]` 循环，用 `getattr(resume, field_name)` 逐个取值——不用写五遍几乎一样的代码。

**Q7: 如果用户想加一条新检查规则，怎么扩展现有代码？**

A: 三步：
① 在 checker.py 里写一个新函数，签名和其他规则一样：输入数据，返回 `{rule, pass, message}` 字典；
② 在 `check_all()` 里加一行 `results.append(新函数(...))`；
③ 主程序不用改，自动生效。
所有规则都有统一的输入输出格式，这也是为什么设计成"每条规则一个函数"——方便扩展，互不影响。

## 第 5 课

**Q1: 你是怎么把大模型接入简历优化工具的？用了什么技术方案？**

A: 我选了 DeepSeek 作为后端模型，用 `openai` 库（Python SDK）通过 OpenAI 兼容接口调用。核心思路是：把前几步的分析结果（关键词匹配结果 + 质量检查结果 + 简历原文）拼成一段结构化的 prompt，发给 DeepSeek，让它生成有针对性的优化建议，而不是泛泛而谈。整个调用封装在 `resume_optimizer/llm.py` 模块里，包括创建客户端、构建 prompt、处理响应三个职责。

**Q2: `system` 消息和 `user` 消息有什么区别？**

A: 在 Chat Completion API 中，`messages` 列表里每条消息都有 `role`（角色）字段。`system` 消息用于设定 AI 的人设和行为准则——比如"你是一位简历优化专家"，它影响 AI 回答的语气和专业度。`user` 消息是具体的问题或任务描述——比如"请帮我优化这段简历"。`system` 定调子，`user` 给具体问题，两者配合让 AI 输出更可控。

**Q3: 为什么 API Key 要放在 .env 文件里，而不是直接写在代码里？**

A: .env 文件里的 API Key 不会被 git 追踪（配合 .gitignore），上传代码时不会泄露到公开仓库。如果直接写在代码里，一旦代码被公开（比如 GitHub），任何人都能拿到我的 key，可能导致 key 被盗用、超额消费甚至被拿去挖矿。.env + .gitignore 是业内通用做法，也是安全性的基本要求。

**Q4: HTTP API 调用和之前读本地文件有什么本质区别？**

A: 读本地文件是在自己电脑上操作，只涉及磁盘 I/O。API 调用是通过网络向远程服务器发送 HTTP 请求并等待响应，涉及网络通信。关键区别：① 有网络延迟和失败的可能（断网、服务器挂了）；② 需要身份认证（API Key）；③ 每次调用有成本（按 token 计费）；④ 所以必须在调用处包 try/except，处理各种可能的错误。

**Q5: temperature 参数是什么？你选了什么值，为什么？**

A: temperature 控制 AI 输出的随机性/创造性，范围 0~1。0 最确定性（每次回答几乎相同），1 最随机（每次都不同、创意更强）。简历优化场景我选了 0.7——需要 AI 有一定创造性来改写措辞，但不能太离谱偏离原意。如果是代码生成或数学题这种需要精确的场景，会设成 0 或接近 0。

**Q6: max_tokens 是什么？为什么不设得很大？**

A: max_tokens 限制 AI 单次回复的最长长度（输入 + 输出总 token 数）。设限制的理由：① DeepSeek API 按 token 计费，不设上限可能烧很多钱；② 简历优化建议不需要太长，设 2000 够用；③ 也是一种安全措施，防止模型出 bug 无限输出。

**Q7: 调用 AI 时为什么包 try/except？具体能兜住哪些错误？**

A: API 调用是"对外部服务的依赖"，有很多不可控因素：① 网络断连 / DNS 解析失败；② API Key 错误或过期；③ 账户额度用完（402/429 错误）；④ 服务器返回非预期格式。任何一项出错都会抛异常导致程序崩溃。包 try/except 后在 except 里打印友好提示并告诉用户检查什么，程序不会因外部故障而崩溃。

**Q8: 你的 prompt 是怎么设计的？为什么要这样设计？**

A: 我的 `build_prompt()` 把四块信息组织成一个结构化的 prompt：① 简历原文（让 AI 知道在改什么）；② 关键词匹配摘要（哪些命中、哪些缺失、匹配率）；③ 质量检查摘要（每条规则的通过/不通过状态）；④ 明确的任务要求（分 4 条，从关键词融入到 STAR 改写）。核心设计思路是：给 AI 的上下文越具体、越有针对性，它返回的建议越可用。如果只给一份简历让 AI"帮忙改改"，建议会很泛。

**Q9: 从项目架构角度看，为什么 LLM 调用要单独放一个模块（llm.py）？**

A: 职责分离原则：parser 管解析、matcher 管匹配、checker 管检查、llm 管 AI 调用，每个模块的职责清晰单一。好处：① 修改 prompt 逻辑只改 llm.py，不动其他地方；② 如果以后换模型（比如从 DeepSeek 换到 OpenAI 或 Claude），只改 llm.py 里的 `base_url` 和 `model` 参数；③ 单元测试时可以 mock 掉 API 调用，不影响其他模块的测试。

## 第 6 课

**Q1: 为什么选择 Streamlit?不用传统的 HTML/CSS/JavaScript?**

A: 我的项目目标是快速做出可用的产品原型,而不是做一个企业级前端项目。Streamlit 让我全部用 Python 写界面,不用学 HTML/CSS/JS 三件套。而且我前面 5 课写的 parser/matcher/checker/llm 四个模块一行都不用改——Streamlit 直接接上它们的返回值,用 `st.success`、`st.markdown` 等方法渲染出来。对 Python 开发者来说,门槛和开发效率都是最好的。

**Q2: Streamlit 和传统 Web 框架（Flask/FastAPI）有什么本质不同?**

A: 传统框架是你自己决定"用户请求来了 → 怎么处理 → 返回什么 HTML",要自己管理路由、模板、状态。Streamlit 把这一切都隐藏了——它的执行模型是"脚本从上到下重跑",用户每次操作（点按钮、打字、上传文件）整个脚本重新执行一遍,Streamlit 自动对比上一次的输出,只更新变了的部分。代价是它的灵活性不如传统框架,但换来的是极快的开发速度。

**Q3: Streamlit 的脚本重跑机制是怎么工作的?**

A: 用户每次交互（比如点按钮、输入文字、上传文件）,Streamlit 都会把整个 Python 脚本从头执行一遍。这和命令行程序"执行完就退出"完全不同——网页在背后是一个"一直循环跑"的进程。关键优化:Streamlit 不会每次都重建整个页面,它会计算"哪些输出变了",只更新变了的组件。所以代码文件改了保存,浏览器页面就会自动刷新。

**Q4: 文件上传和读本地文件有什么区别?**

A: 第 1 课读本地文件时需要提供磁盘路径 → `open(path).read()` 拿到文本。网页上的文件是用户上传的,文件在内存里,没有磁盘路径。Streamlit 的 `st.file_uploader` 返回的是 `UploadedFile` 对象 —— `.read()` 拿到字节(bytes),再用 `.decode("utf-8")` 转成字符串。一个返回字节一个是文本,这是核心区别。生活例子:本地读文件是从书架上取一本书看;上传文件是别人把书递到你手上。

**Q5: 你在用 Streamlit 做界面时,页面是怎么布局的?**

A: 我用 `st.sidebar` 把所有输入控件(文件上传、关键词输入框、模型下拉选择、提交按钮)放在左侧边栏,主区域放输出结果(匹配结果、质量检查、AI 建议)。这样用户的视线从左向右,操作流程清晰:左边输入,右边看结果。布局逻辑:输入区干扰项少,输出区足够宽敞能显示大段的建议文本。

**Q6: 你提到之前的四个模块一行没改,为什么能做到?**

A: 因为第 2~5 课写 parser/matcher/checker/llm 时,每个模块的职责是"拿数据 → 返回数据",没有直接 print 或者依赖任何具体的显示方式。print 是在 main.py 里做的。到了做网页时,我把 main.py 里的 `print(结果)` 全部换成 `st.success(结果)`、`st.error(结果)`、`st.markdown(结果)` 等 —— 内核没变,只换了外壳。这就是模块化设计的好处:换界面、加功能都不动内核逻辑。

**Q7: `st.spinner` 和 `try/except` 在 AI 调用部分起什么作用?**

A: 调 DeepSeek API 是项目里最慢的一步(通常要 5-15 秒),如果页面什么反应都没有,用户会以为卡死了。`st.spinner` 在等待期间显示一个转圈动画 + "AI 正在分析"的文字,告诉用户"我在干活"。`try/except` 兜住网络错误、Key 错误、额度耗尽等各种 API 调用可能出的问题,保证即使 AI 挂了,页面也不会崩溃,而是一条红色的友好提示告诉用户怎么排查。

**Q8: 你怎么发现并解决了 AI 回复被截断的问题?**

A: 第一次做网页时,发现 AI 建议写到一半就不动了,末尾没有结论,用户体验很差。我排查后发现是 `llm.py` 里 `max_tokens=2000` 设太低了,prompt 里给 AI 提了 5 条任务要求,2000 token 输出不完就被硬截。我做了两件事修复:① 把 `max_tokens` 从 2000 提到 4096,一般请求够用了;② 调完 API 后检查 `response.choices[0].finish_reason` —— 如果值是 `"length"`(说明被 max_tokens 截断),就在输出末尾追加一行红字警告"回复因字数限制被截断"。这样即使以后再截,用户也不会一头雾水。

**Q9: `finish_reason` 是什么?什么时候用它?**

A: 每次调用 OpenAI 兼容的 Chat Completion API,响应里都有一个 `finish_reason` 字段,说明这次回复是怎么结束的。两种常见值:`"stop"` 表示 AI 自然说完了,是正常结束;`"length"` 表示 AI 被 `max_tokens` 限制卡住了,话没说完就被强制停止。在商业产品里检查这个字段是基本操作——不应该让用户看到半截的话。如果发现是 `"length"`,至少要在 UI 上给用户一个提示。

**Q10: 你的项目文件结构是怎么组织的?为什么这样分?**

A: 项目根目录下 `resume_optimizer/` 是一个包,里面按职责拆了 4 个模块:parser(解析)、matcher(匹配)、checker(检查)、llm(AI 调用)。入口有两个:早期的 `main.py`(命令行版本)和现在的 `app.py`(网页版本),它们各自负责"把模块串起来 + 输出"。`examples/` 放测试数据,根部还有 `.env`(密码)、`NOTES.md`(笔记)、`INTERVIEW.md`(面试)。整体思路是"每个模块做一件事、做好一件事",这也是面向对象/函数式编程里最核心的设计原则——单一职责。

## 第 7 课

**Q1: 你的项目有测试吗?是怎么写的?**

A: 有,用 pytest 写了 23 个单元测试,覆盖三个核心模块:解析(parser)、关键词匹配(matcher)、质量检查(checker)。测试的组织遵循 pytest 的约定:`tests/` 文件夹里放 `test_*.py` 文件,每个文件里是 `test_*` 函数。核心手法是"出题"——给每个函数构造合格和不合格的数据,验证它的判断对不对。运行只需一行 `.venv/Scripts/python -m pytest tests`。

**Q2: assert 是什么?为什么测试都要用它?**

A: assert 是 Python 的关键词,`assert 条件` 的意思是"我断定这个条件为真"——条件为 True 程序安静继续,为 False 立刻抛错停下。测试的本质就是把对函数的期望写成 assert:比如 `assert result["hit"] == ["Python"]`,如果函数行为符合期望,测试通过;如果哪天有人改代码把函数改坏了,跑测试立刻报警。它是测试的"探针"。

**Q3: 测试跑失败了你怎么办?这是一个关键的工作习惯问题。**

A: 我第一反应不是慌,而是先判断**是代码坏了,还是测试写错了**。我实际就踩过这个坑:第一次跑测试,23 个里挂了 2 个,排查后发现两个都是我自己写测试时犯的错——一个把期望匹配率算错了(3/4 写成 0.5),一个测试数据造太短(没到 300 字的要求)但代码本身是对的。如果看到红色就慌着去改产品代码,反而会把对的改坏。所以我的工作习惯是:测试红了,先读失败信息判断问题在哪边,再动手。

**Q4: 你为什么只测 parser/matcher/checker,不测 llm.py?**

A: 这是测试边界意识。llm.py 要调用 DeepSeek 的真实 API,测它意味着测试要依赖网络、要花钱、还要有 API Key,这会让测试不稳定也不可复现。我只测"稳定、纯逻辑、不花钱"的部分。如果需要测 llm.py,正确做法是 mock(模拟)掉 API 调用,但这超出当前项目的必要了。

**Q5: 为什么要单独做一个 config.py?什么是"魔法数字"?**

A: 魔法数字/魔法字符串是指直接裸写在代码里的值,比如 `temperature=0.7`,它没有名字,读代码的人不知道 0.7 是干嘛的,想改还得在多个文件里找。我把项目里所有可调参数(模型列表、默认模型、温度、max_tokens、默认路径、默认关键词)集中到 config.py 变成有名字的常量,其他文件 `from config import xxx` 引用。这样改一个参数只改一处,全局生效。这也是一个工程化习惯:可配置的东西不应该散落在业务代码里。

**Q6: 你的 README 是怎么写的?为什么重要?**

A: README 是 GitHub 仓库的门面,面试官筛项目第一眼就看它。我按这个结构写:一句话介绍项目是什么 → 功能列表 → 快速开始(建环境、配 API Key、运行只要三步)→ 怎么跑测试 → 项目结构 → 技术栈 → 配置说明。配套还写了 `.env.example`(API Key 模板)和 `requirements.txt`(依赖清单),保证别人拿到仓库能跑起来。没有 README 的项目,面试官可能点开就划走了。

**Q7: 如果以后给项目加新功能,你怎么保证不把旧功能改坏?**

A: 这正是我写测试的意义。测试是"安全网":改代码前先跑一遍测试确认全绿,改完再跑一遍,如果红了说明改坏了,定位到具体失败用例就知道哪里出问题。这让我可以放心地迭代——加新功能、重构代码都不怕破坏已有行为。这也是我在项目里养成的工程习惯:先铺测试,再改代码。

## 第 8 课

**Q1: 你做了"AI 自动提取 JD 关键词",是怎么实现的?**

A: 我之前是让用户手动填 JD 关键词。这次升级改成:用户上传或粘贴职位描述文本,我调用 DeepSeek,在 prompt 里要求它"只返回一个 JSON 数组"的关键词列表,再用 `json.loads` 把返回解析成真正的 Python 列表,最后交给原来的 `match_keywords` 做匹配。整个流程复用了我已有的 DeepSeek 接入,只新增了一个提取函数和一个解析函数。

**Q2: 什么是"结构化输出"?为什么用它?**

A: 结构化输出是让大模型不只返回"给人看的文字",而是返回"程序能直接解析的数据",比如 JSON 数组。第 5 课的优化建议是人读的文本;但关键词提取这一步,程序必须拿到能用的列表,不能拿一段文字回来自己猜。所以 prompt 里明确要求"只返回 JSON 数组,形如 [\"Python\", \"API\"]"。这是 LLM 应用里非常核心的技巧——模型输出要"程序友好",才能接进后续逻辑。

**Q3: 你怎么处理 AI 回复不"老实"的情况?**

A: AI 有时会画蛇添足:返回带 markdown 代码块的 ```json [...] ```,或者前后夹带"提取到的关键词如下:..."这类解释文字。所以我写了 `parse_keyword_json` 做容错:先直接 `json.loads` 整段,失败就用正则 `re.search(r"\[.*\]", text, re.DOTALL)` 抠出方括号部分再解析;还解析不了就抛 `ValueError`,让上层给用户友好提示,而不是静默返回垃圾数据。

**Q4: 提取关键词和生成建议,为什么温度设置不一样?**

A: 生成改写建议用 `temperature=0.7`,因为需要 AI 有一定的创造性来润色措辞;提取关键词用 `temperature=0.3`,因为提取要稳定、可复现——同样的 JD 每次提取结果应该尽量一致,太"创意"反而会提取出不同的词。规律是:任务越要准确稳定,温度越低;越要创意发散,温度越高。

**Q5: 提取关键词这个功能,测试是怎么写的?**

A: 我只测了解析函数 `parse_keyword_json`,没测调用 API 的 `extract_keywords_from_jd`。因为前者是纯逻辑不花钱,后者要调真实 API(依赖网络、要花钱、还要有 Key)。我把一个函数拆成"调 AI"和"解析"两个,就是为了让纯逻辑部分可测试——这是我在第 7 课学到的测试边界意识的应用。解析函数我写了 7 个用例,覆盖干净 JSON、markdown 代码块、夹带文字、空数组、过滤空字符串、纯垃圾抛错等情况。

**Q6: 网页端这个功能的用户体验是怎么设计的?**

A: 侧边栏新增了"上传 JD 文本"和"粘贴 JD 文本"两个入口(二选一)。点分析后,先转圈让 AI 提取关键词,然后把提取结果用 `st.multiselect` 多选框展示出来,用户能勾选/取消勾选保留哪些关键词,再往下做匹配。这样用户对 AI 的结果有"确认权",不会被 AI 的提取结果牵着走。没传 JD 时,还保留原来的手动填关键词作为兜底。

**Q7: 如果 JD 是图片(截图),能处理吗?为什么现在没做?**

A: 目前只能处理文本。因为 DeepSeek 的对话 API 是纯文本模型,不收图片。图片要处理需要先做 OCR(把图片里的字认出来变成文本),再走现有的文本流程,这需要额外引入一个 OCR 库。这是个明确的后续方向,但当前先做通了文本链路,图片作为增量再补。

## 第 9 课

**Q1: 你这个项目怎么"导出优化版简历"?**

A: 之前 AI 只给优化建议(给人看的文字)。这版升级让 AI 直接输出一整份改写后的简历:我把原有的匹配结果和检查结果拼成一个新 prompt(`build_rewrite_prompt`),要求 AI"输出完整的新简历全文,不是建议、不是示例",调 DeepSeek(`get_rewritten_resume`)拿到成品文本,再写一个 exporter 模块(`save_resume`)把它存成 UTF-8 文件。命令行版存到 `output/optimized_resume.txt`,网页版用 `st.download_button` 直接在浏览器下载。

**Q2: 文件写入怎么实现的?和读取有什么区别?**

A: 就一个字母的区别。读取用 `open(path, "r")`,写入用 `open(path, "w")`(write)。`"w"` 模式文件不存在会新建,存在会整个覆盖,所以每次跑都得到最新内容。两者都用 `with open(...) as f`,文件用完自动关闭,防止资源泄漏;都要指定 `encoding="utf-8"`,不然写中文会乱码。另外写入前我用 `os.path.dirname` + `os.makedirs` 保证目标文件夹存在,否则 `open()` 会报错。

**Q3: 你要求 AI 输出时保留固定的分段标题,为什么?**

A: 因为导出不是终点。我的 `parse_resume()` 靠几个固定标题逐行精确比对来切段,标题保住了,导出的文件还能再被工具读回来分析,形成"改→查→再改"的闭环,而不是导出就死路一条。这是 LLM 应用里很重要的一个意识:AI 的输出格式要"程序友好",要和下游工具约定好,不能只给人看。

**Q4: 实际跑的时候,AI 有没有不听话?你怎么发现的?**

A: 有,而且是第一次真机验证抓到的。导出后我把文件喂回 `parse_resume()`,发现 header 有 27 行、专业技能/教育经历/项目经历/个人优势全空——说明标题没被识别。打开文件一看,AI 把标题写成了 `**专业技能**`,给标题加了 markdown 粗体星号,而解析器比的是精确字符串"专业技能",不认,整段内容全掉进 header。我是靠"导出后回读验证"这一步抓住它的,肉眼很难看出来——这一步就是我的"验收标准"。

**Q5: 那你怎么修的?**

A: 程序逻辑没错,错的是 prompt 没把要求说死。我只写了"保留标题一字不差",没禁止加装饰,AI 就自作主张加了 `**`。我把 prompt 第 2 条改成"标题只写纯文字,禁止 ** 星号、# 号等任何 markdown 标记",重跑后回读,五段内容全部正确。教训是:prompt 要求越具体,输出越可控——和第 8 课"AI 不老实要做容错"是同一类问题,这次选择在源头(prompt)就堵上。

**Q6: 这个功能让你学到什么工程思想?**

A: 两件事。一是 DRY(不重复自己):`build_prompt` 和 `build_rewrite_prompt` 都要把匹配+检查结果压成摘要,我抽成了公共函数 `_summarize_analysis`,改一处全局生效,函数名前加 `_` 表示内部函数。二是"输出格式兼容下游":产品给用户的交付物(AI 输出)要能被自己的后续工具消化,这才是闭环,这直接影响了我对 prompt 的设计。

**Q7: 为什么命令行和网页版用的模型不一样?**

A: 命令行版没有模型选择框,直接跑一条龙,所以重写用默认的 `REWRITE_MODEL`(`deepseek-v4-pro`),质量更高;网页版侧边栏用户已经手动选了模型,改写就跟着用户选走,保持用户可控。都通过函数参数传递,默认值集中在 config.py。

**Q8: 新功能怎么保证不把旧功能改坏?**

A: 改之前先跑一遍 30 个测试全绿,改完再跑,加上新增的 3 个导出测试共 33 个全绿。测试是安全网:导出功能只新增了 exporter 模块和两个新函数,没动已有函数的行为;唯一动过的是 `build_prompt`(把内联摘要换成调用公共函数),跑测试确认它没坏。

## 第 10 课

**Q1: 什么是 RAG?用一句话讲给外行听。**

A: RAG 就是"先查资料再回答"。普通大模型是闭卷考试,凭训练时记住的知识答题;RAG 相当于开卷考试,在回答前先从一个知识库(比如一摞真实的岗位要求文档)里检索出和当前问题最相关的内容,拼进提示词,再让模型参考着回答。拆开是三个词:Retrieval 检索、Augmented 增强、Generation 生成。

**Q2: 你的简历优化工具为什么要引入 RAG?**

A: 因为以前 AI 改简历是"闭卷"的。它只知道简历缺了哪些关键词,但它不知道我现在投的这份岗位到底要什么,全靠常识猜。引入 RAG 后,我建一个"岗位知识库"——里面是真实的 AI 岗 JD,AI 改写简历前先从库里检索出和当前 JD 最相关的几段真实岗位要求,拼进 prompt,这样改出来的简历才跟真实岗位对齐,而不是空想。打个比方:从"背了 100 道题"变成"做题前先翻书看相关章节"。

**Q3: 你的知识库是怎么组织的?**

A: 知识库就是 `examples/knowledge/` 目录下一摞 .txt 文件,一份文件一份真实岗位 JD。我先放了 3 份示例文档把框架跑通,以后可以把我投过的真实 JD 都放进去,积累越多检索越准。`read_knowledge_files()` 用 `os.listdir` 扫描目录,把每个 .txt 都读进来,返回文档列表。这样加新文档 = 丢个文件进去,不用改代码。

**Q4: 为什么要把文档切成小块?切块的原则是什么?**

A: 因为一份 JD 可能几百字,如果整篇都塞给 AI 上下文又长又不准。切成小块后,检索才能精确到"最相关的那一小段",而不是整篇。切块原则是按"内容边界"切,而不是按字数硬切:先按空行拆段落(空行是天然的语义分界,比如【岗位职责】和【任职要求】之间),段落里的一行就是一个内容点,一行太长再按中文标点(，。、；)切,避免把一句话腰斩。

**Q5: 切块里用了什么 Python 技巧?**

A: 三个点比较有意思。一是 `os.listdir` + 列表推导式,一行搞定"读目录里所有 .txt";二是 `str.rfind(子串, 起点, 终点)`,从右往左找标点位置,这样切块时能停在最后一个标点后面,不会把句子切碎;三是 while 循环,一行没切完就继续切,每次取前 chunk_size 字符、剩下的接着切,直到切完。

**Q6: RAG 的完整链路是什么?你项目现在走到哪一步了?**

A: 完整链路是:文档解析 → 切块 → 向量化 → 相似度检索 → 拼 prompt → 生成。我项目现在完成了前两步:知识库建好、切块函数写好。向量化、检索、接入改写流程是接下来的三课(11~13 课)。检索我们会自己实现词频权重 + 余弦相似度,不用 DeepSeek 的 embedding API,这样能亲手讲清原理,正好补上简历里 Egg 项目"讲不明白"的 RAG 部分。

**Q7: 用 RAG 会不会花更多 API 钱?**

A: 不会。切块和向量化检索都是本地代码算的,不调任何 API,零成本。只有最后"拼好 prompt 发给 AI 生成"那一步才花钱——但那一步本来就要调。RAG 反而是省钱的:它让 AI 有了正确的参考,减少了 AI 靠试错猜测产生的无效输出,还可能减少重跑次数。

**Q8: RAG 的常见坑你知道吗?**

A: 一个经典的坑是"检索到的内容不相关还硬拼进去",AI 可能被无关内容带偏,反而变差。所以检索质量很重要——这就是为什么切块策略、相似度算法都值得精心设计。另一个坑是切块太碎会丢失上下文,太粗又不够精准,要找到平衡。这些都是第 11 课做检索时要重点验证的。

## 第 11 课

**Q1: 什么是向量?为什么检索时需要把文字变成向量?**

A: 向量就是"一串数字",代表一段文字在数学空间里的"含义坐标"。就像地图上每个城市用经纬度定位,文字也可以用一个向量来定位——两个向量方向越近,含义越相似。变成向量之后,就可以用数学公式(余弦相似度)精确计算"这两段话有多像",而不是靠关键词硬匹配。我的项目用 TF-IDF 做向量化:每个词在知识库里的重要程度(TF × IDF)决定向量里对应位置的数字大小。

**Q2: TF-IDF 是什么?为什么不用更复杂的模型?**

A: TF-IDF 是"词频(Term Frequency)× 逆文档频率(Inverse Document Frequency)"。思路:一个词在这段文字里出现多次(TF 高),但在整个知识库里很少见(IDF 高,说明它是特征词不是通用词),那这个词的权重就高。比如"FastAPI"在 JD-A 里出现 5 次,整个知识库 58 块里只有 3 块提到它 → TF-IDF 给"FastAPI"打高分 → 向量里这个位置就是个很大的数。

为什么先用 TF-IDF:① 纯统计方法,不需要神经网络,不需要 GPU,不需要调 API,上手就能跑;② 能亲手实现余弦相似度、点积、向量长度这些底层概念,面试时有东西可讲,而不是"调了个 embedding API 但不知道原理";③ 以后换更强的模型时,只需要替换向量化那一步,检索逻辑(余弦相似度 + top_k)完全复用。

**Q3: 余弦相似度怎么算?为什么用余弦而不是直接比距离?**

A: 公式是 `cos(θ) = (A·B) / (|A| × |B|)`。分子是点积(两个向量对应位置相乘再求和),分母是两个向量各自长度(平方和开根号)的乘积。结果在 [-1, 1] 之间:1 = 方向完全一致,0 = 垂直(无关),-1 = 完全相反。

用余弦而不是欧几里得距离的原因是:余弦只比"方向",不比"长度"。长文档(字数多、向量里非零位置多)天然点积会更大,如果不用分母归一化,长文档会被优先排到前面,但排名的标准应该是"意思像不像",而不是"字数多不多"。除以向量长度后,长短文档站在同一起跑线上,只比语义方向。

**Q4: 点积是什么?在你的代码里怎么实现的?**

A: 点积就是两个向量"对应位置相乘再求和"。比如 `a = [1, 3, -5]`, `b = [4, -2, -1]`,点积 = 1×4 + 3×(-2) + (-5)×(-1) = 4 + (-6) + 5 = 3。代码里用 `for x, y in zip(a, b): total += x * y` 实现,`zip` 把两个列表配对——a 的第一个和 b 的第一个、a 的第二个和 b 的第二个……挨个乘、累加。生活例子:两个人的购物篮重合度——A 买了 2 苹果 1 水,B 买了 1 苹果 2 水,对应位置乘起来再求和,数字越大越像。

**Q5: 你项目里的检索流程是怎样的?每一步在干什么?**

A: 三步:

① `build_knowledge_index(chunks)` — 把知识库所有块"索引化":用 sklearn 的 `TfidfVectorizer` 一次性把所有块转成向量矩阵,返回一个字典(原始块列表 + 向量矩阵 + 向量化器)。建索引只做一次,后续检索复用。

② `retrieve(query, index, top_k=5)` — 检索:先把查询文本用同一套 vectorizer 转成向量(保证词汇表一致),然后跟索引里每一块逐个算余弦相似度,按相似度从高到低排序,取前 top_k 个返回。

③ 返回结果是 `[{"text": "块内容", "score": 0.85}, ...]`,下游直接拼进 prompt 喂给 AI——这就是 RAG 的"增强"步骤。

整个流程纯本地计算,不调 API、不花钱、不联网。

**Q6: 你为什么只测了余弦相似度和点积,没测 build_knowledge_index 和 retrieve?**

A: `cosine_similarity`、`dot_product`、`vector_norm` 是纯数学函数——输入数字、输出数字,没有外部依赖,跟 parse_keyword_json 一样属于"稳定、不花钱"的纯逻辑,最适合写测试。

`build_knowledge_index` 和 `retrieve` 虽然也不联网,但依赖 sklearn 的 `TfidfVectorizer`,输出的向量矩阵内部结构复杂(稀疏矩阵),不适合写简单的 assert。如果硬要测就得 mock sklearn,投入产出比不高。这也是第 7 课讲的测试边界:测"自己能完全控制"的部分,外部库的行为默认它是正确的。

**Q7: 你的测试遇到了浮点数精度问题,怎么回事?**

A: 我写 `test_cosine_opposite` 时断言 `cosine_similarity([1,2], [-1,-2]) == -1.0`,结果实际返回 `-0.9999999999999998`。数学上确实是 -1,但计算机用二进制存小数时存在微小误差。差了 0.0000000000000002,pytest 就判定不相等。

我用 `pytest.approx(-1.0)` 替换了 `== -1.0`,做近似比较而非精确比较。规律:涉及浮点数(小数)的比较,永远不要用 `==`,要用 `pytest.approx()` 或 `math.isclose()`。

**Q8: `zip()` 和 `lambda` 在检索代码里怎么用的?**

A: `zip(a, b)` 把两个列表"拉链"配对——`zip([1,2,3], [4,5,6])` → `(1,4), (2,5), (3,6)`。我在 `dot_product` 里用它把两个向量的对应位置配对,一行循环搞定对应相乘再求和。

`lambda` 是匿名函数,一行写完的小函数,不需要 def + 函数名。检索里 `scores.sort(key=lambda pair: pair[1], reverse=True)` 的意思是:按每对 (索引, 相似度) 的第二个元素(相似度)排序,从大到小。这个 lambda 等价于 `def get_score(pair): return pair[1]`,但更简洁,适合这种只用一次的小逻辑。

## 第 12 课

**Q1: 你的 RAG 完整链路是怎么串起来的?每一步在干什么?**

A: 三步:检索(Retrieve)→ 增强(Augment)→ 生成(Generate),我的项目里对应:

① **检索**:`build_knowledge_context(jd_text, top_k=3)` 一条龙完成——读知识库文档(`read_knowledge_files`)→ 切成小块(`chunk_text`)→ 建 TF-IDF 索引(`build_knowledge_index`)→ 拿用户的 JD 算余弦相似度,取最相关的 3 块(`retrieve`)。

② **增强**:把检索到的 3 块岗位要求拼成一段【岗位知识参考】,注入给 AI 的 prompt(`build_prompt` / `build_rewrite_prompt`)。

③ **生成**:AI 参考这段真实岗位要求,输出优化建议 / 整份改写简历。

一句话:用户上传 JD → 从知识库捞出"与这份 JD 最匹配的真实岗位要求" → 当参考书给 AI → AI 照着改。

**Q2: "增强"具体怎么实现?为什么用 prompt 注入而不是别的?**

A: 就是把检索结果**拼进 prompt 字符串**。我在 `build_prompt` / `build_rewrite_prompt` 里加了一个可选参数 `knowledge_context`(默认空串):有值时在【当前简历内容】和【分析摘要】之间插一段【岗位知识参考】,里面是编号的检索结果;空串时整段不出现。

用 prompt 注入是最简单可靠的接入方式——LLM 的输出只受 prompt 约束,把参考信息写进 prompt 就等价于"让 AI 开卷考试"。不需要改模型、不需要改 API,只是输入侧多了一段文字。

**Q3: 为什么给参数设默认值 `knowledge_context=""`?**

A: 向后兼容。第 9 课就有的 `get_rewritten_resume(resume, match, check, model)` 调用,新增参数后如果必须传,那所有旧调用点都要改。设了默认空串,旧调用一行不改、行为不变;想用 RAG 的调用方显式传参考即可。这也是工程上的通用做法:给函数加参数时,用带默认值的方式降低对既有代码的破坏。

**Q4: 加了参考材料之后,你的程序出了什么问题?怎么发现、怎么解决的?**

A: 端到端跑真实调用时发现 AI 输出被截断——优化建议返回空、改写简历断在"工具"二字,而且截断警告行混进了导出的简历文件。原因是加了参考书后 prompt 变长,AI 输出也更啰嗦,原来的 `max_tokens`(建议 4096、改写 8192)不够了。`finish_reason` 返回 `"length"` 就表示被硬截断。

修复:把 config.py 里 `MAX_TOKENS` 提到 8192、`REWRITE_MAX_TOKENS` 提到 16384,重跑后输出完整。

这个坑说明一个规律:**RAG 增强 = 输入变长 → 输出通常也变长,要同步检查 token 预算**,这也是实际部署时调参的真实经验。

**Q5: 你的 RAG 相比"不检索、直接让 AI 改写",好在哪里?**

A: 两个好处:① **贴合真实要求**——AI 不再凭想象改,而是参考我从真实岗位要求知识库(投递的那些 JD)里检索出的、与这份 JD 最相关的几段要求来写,输出的简历针对性更强;② **减少幻觉**——没有参考时 AI 可能编造不存在的岗位要求或夸大技能,有了真实要求当锚点,编造空间被压缩。

类比:闭卷考试 vs 开卷考试。开卷不是作弊,是让模型在"有据可依"的情况下发挥。

## 第 13 课

**Q1: 你的 RAG 相关代码怎么写测试?为什么不用真实知识库直接测?**

A: 分两层。`build_rewrite_prompt` 是纯函数(字符串进、字符串出),直接测三个场景:传参考 → prompt 里含【岗位知识参考】;不传 → 不含;传空串 → 等同不传。

`build_knowledge_context` 默认会读磁盘上的真实知识库,直接测会依赖真实文件——哪天我往库里加一份文档,测试结果就变了,不稳定。所以用 pytest 的两个内置道具:**`tmp_path`** 给每个测试发一个临时文件夹,我在里面自己写两份文档(一份 Python 后端、一份前端);**`monkeypatch`** 临时把知识库路径指到那个临时文件夹,测完自动还原。这样测试完全不碰真库,数据完全可控。

**Q2: 测试里你踩过一个"预期太具体"的坑,讲一下?**

A: 我第一次断言"检索结果必须包含【岗位名称:Python 后端开发工程师】那一块",结果失败——因为我的 JD 里写了 FastAPI,"要求:熟悉 Python、FastAPI、MySQL"这块和 JD 的重合度更高,相似度分数排第一,被选中了。其实检索逻辑完全正确,是断言写死错了。

教训:写断言时别猜"具体哪一块会被选中",要断言"行为正确"——比如"搜到的是 Python 相关的、不是无关的前端"。这样测试才稳,不会因为检索细节变化就误报。

**Q3: 你的项目现在测试情况怎么样?**

A: 54 个 pytest 用例全绿,覆盖解析、匹配、检查、关键词 JSON 解析、文件导出、向量检索、prompt 拼接、知识库检索。原则是只测"纯逻辑、不联网、不花钱"的部分——调真实 API 的环节(如 `get_rewritten_resume`)不写测试,因为测试不该依赖网络、不该花 token(第 7 课讲的测试边界)。

**Q4: RAG 整个升级做完,你最后怎么确认"没有弄坏原来的功能"?**

A: 两条:① 全量跑 pytest,54 个用例全绿,原有解析/匹配/检查/导出功能没被改坏;② 真实跑一次命令行端到端,确认 AI 建议、改写、导出都正常,而且导出的简历明显融入了知识库岗位要求。测试保证"没退化",端到端保证"新功能真的能用"。

## 项目介绍话术(实战训练 2026-08-13)

> 这节是"面试怎么讲项目"的训练记录。原则:不是背,是**用自己的话讲**;
> 合上文档能讲出来才算会。每轮训练往里加一问。

### 60 秒版自我介绍

> 这个项目是一个**简历优化助手**,帮助求职者修改简历使其更符合岗位需求。一共三大块:
>
> **第一,本地规则**。在本地解析简历和 JD,查看简历与岗位的匹配度,以及检查简历自身的质量——这块不联网、不花钱。
>
> **第二,接入 DeepSeek 大模型**。让它从 JD 自动提取关键词、生成优化建议,以及直接生成一版修改好的简历。
>
> **第三,RAG 知识库**。把真实的岗位要求建成知识库,改写时先检索出与这份 JD 最相关的几段,作为参考资料拼给 AI,让它开卷考试,生成更符合岗位市场需要的简历。**这个检索是我自己实现的 TF-IDF 向量化加余弦相似度,没有调现成的 embedding API。**
>
> 最后用 **Streamlit 做成网页版,配了 54 个 pytest 测试**。整个项目每一层原理我都能讲。

### 深挖题 1:为什么 RAG 不用 embedding API,要自己写 TF-IDF?

**提问场景**:面试官听到"自己实现 TF-IDF 检索",追问为什么不直接用现成的。

**金句版回答**:
> "两个原因。**第一是成本**——我现在做项目要控制成本,本地 TF-IDF 零调用费、零联网依赖,跑多少遍都不花钱。**第二是我想真正理解原理**——如果直接用现成的 embedding API,向量化这步就是个黑盒,我说不清里面的数字怎么来的;自己实现一遍 TF-IDF 和余弦相似度,每一步都能讲清楚。而且我把检索封装成了 `retrieve()` 接口,以后想换更强的 embedding 模型,**只需要替换向量化那一步,检索、排序、注入 prompt 这些下游逻辑完全不用动**。"

**要点**:① 成本 ② 理解原理 ③ 接口可替换。**别**说"调 API 没技术含量"——面试官可能就是调 API 的人,显得轻浮。

### 深挖题 2:讲一个你踩过最大的坑

**提问场景**:面试官验证"你是否真做过"的最爱问题。坑要讲出「现象 → 排查 → 原因 → 解决 → 规律」完整链条。

**金句版回答**:
> "最大的坑是 **AI 输出被截断**。现象:加了 RAG 知识库之后,AI 改写简历会输出到一半就断——优化建议返回空,改写后的简历断在一个词中间。
>
> **排查**:我检查 AI 返回的 `finish_reason` 字段,发现是 `"length"`,说明被 `max_tokens` 限制了输出长度。
>
> **原因**:加了岗位知识参考之后 prompt 变长了,AI 的输出也跟着变多,原来的 token 上限不够了。
>
> **解决**:把 `MAX_TOKENS` 从 4096 调到 8192、`REWRITE_MAX_TOKENS` 从 8192 调到 16384,再跑就完整了。
>
> **规律**:RAG 增强不只是输入变长,输出通常也变长,加参考内容时要同步检查 token 预算。这让我以后做 AI 应用,都会先想清楚输出长度预算。"

**要点**:① 排查要说"用 `finish_reason == length` 判断截断"(懂 API 结构的信号);② try/except 只是兜底不崩,不是解决截断的关键;③ 用"规律总结"收尾(加分)。

### 深挖题 3:你的 RAG 检索具体怎么一步步工作?

**提问场景**:确认你做过 RAG 后的核心追问,检验对"向量检索"是否真懂(不是只会说名词)。

**金句版回答**:
> "知识库这边,我先做**一次性准备**:把每份岗位要求文档切成一张张卡片(切块),再给每张卡片算一个向量指纹存起来。用户上传 JD 后,把 **JD 整体变成一个向量当'问题'**,拿它跟知识库里每张卡片的指纹比相似度,按相似度从高到低排序,**取最相关的 3 段(top_k=3)**,作为参考材料拼进 prompt。AI 参考这几段真实岗位要求,输出优化建议或整份改写简历。"

**要点**:
- 切块对象是**知识库文档**(不是 JD);JD 是"问题",整体拿去比
- 分两阶段讲:准备(切块+向量化,只做一次)/ 查询(JD 向量化→比相似度→排序取 top_k)
- 强调**向量检索 ≠ 关键词匹配**:关键词是"抠字眼只认字",向量是"看意思,字不重叠意思相近也能命中"
- top_k = "取最相关的前 k 个",代码里是排序后切片 `scores[:top_k]`

### 深挖题 4:项目怎么部署上线的?(2026-08-15 新增,待练)

**提问场景**:简历上写了"已部署上线",面试官很可能问"怎么部署的"。2026-08-15 刚做完部署,这题要趁热练。

**待练(下次回来磨成金句)**,可从这几个事实组织语言:

- 部署链路:写完代码 → git init/add/commit 本地存档 → 推到 GitHub 公开仓库 → Streamlit Community Cloud 连 GitHub 一键部署 → 得到线上网址
- 用 `gh` 命令行建仓库、推送(浏览器打不开 GitHub 时,命令行能连,全程绕开浏览器)
- **密钥处理是重点**:云端没有 `.env`,要在 Streamlit 的 Secrets 里配 API key
- **踩坑:TOML vs .env 格式**——`.env` 是 `KEY=值`(不带引号),TOML 要求 `KEY = "值"`(必须带引号),粘错报 `Invalid format: please enter valid TOML`
- 部署后把 app 设为 public(Anyone with link),陌生人不用登录就能访问
- 面试可讲的价值:会部署 = 不只是写代码,能让产品被用户用起来,中小厂看重的"能落地"
